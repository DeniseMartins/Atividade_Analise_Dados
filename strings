int main() {

    char str[] = "sator";
    char temp;
    int i, j;

    i = 0;
    j = strlen(str) - 1;

    while (i < j) {
        temp = str[i];
        str[i] = str[j];
        str[j] = temp;
        i++;
        j--;
    }


    assert(strcmp("rotas", str) == 0);

    return (0);
}
